[package]
name = "protocol"
version = "0.1.0"
edition = "2021"
description = "zombie-raid wire protocol v1 - single source of truth shared by the Rust server and (later) the gdext client transport"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
bincode = "1"
